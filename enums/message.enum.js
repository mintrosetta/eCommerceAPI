const enumMessages = Object.freeze({
    SUCCESS: 'Successful',
    FAIL: 'Request Failed',
    CREDENTIALS_INVALID: 'Credentials is invalid',
    CREDENTIALS_TOKEN_INVALID: 'Credentials token is invalid',
    CREDENTIALS_TOKEN_EXPIRE: 'Credentials token is expire'
});

module.exports = enumMessages;